{
  "name": "finance-os-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "EXTRACTION SLICE 6 — THE TERMINAL PROOF. A brand-new, clean-room FINANCE OS consumer that runs a finance goal to DONE and a stall to HALTED+summon through the VENDORED Delivery OS governance-engine, importing ZERO rumah-admin / property-lead-os Runtime logic. Proves Delivery OS is the platform and rumah-admin is only a consumer.",
  "scripts": {
    "finance:proof": "tsx run-finance-os.ts",
    "plos:bonus": "tsx run-plos-lead-bonus.ts",
    "scan:zero-admin": "node scripts/scan-zero-admin-imports.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
