# API Contract — <endpoint/service> v<N>

> The contract is the source of truth. Code conforms to this; contract tests enforce it.
> **v4 (executable-contracts):** this document lives **in the PRODUCER's repo**, is **fixture-bearing**, and is
> kept alive by a **consumer-CI heartbeat** — a contract whose fixtures aren't exercised by a consumer's CI is
> presumed dead. One contract per seam; freezing it BEFORE the other side designs is the point (the recorded
> failure: two frozen contracts for the same seam, created in parallel the same day, contradicting each other
> on transport, vocabulary, persistence, and join keys — with neither citing the other repo).

## Endpoint(s)
`<METHOD> /path` — <purpose>

## Request
- Auth: <scheme> · Headers: <…> · Idempotency-Key: <required for state/money writes?>
- Body schema: <typed/Zod/OpenAPI ref>

## Response
- Success: <status + schema> · Pagination: <…>
- **Error model (standard):** `{ code, message, details? }` — codes: <…>

## Semantics
- Idempotent? <yes/no> · Side effects: <…> · Rate limit: <…>

## Versioning & compatibility
- Version: v<N> · Breaking change policy: new version + deprecation window <…>
- Backward-compatible additions allowed; breaking changes are **not** silent.

## Webhooks (if any)
- Signature verification: <…> · Replay protection: <…>

## Fixtures (producer-side — the executable half)
- Fixture files: `<path in THIS repo — real request/response examples a machine can replay>`
- Generated by: `<script>` · Refreshed when: <the contract version bumps>

## Consumer-CI heartbeat
- Consumers exercising these fixtures in their CI: `<consumer repo → CI job>` *(empty = this contract is presumed dead)*
- A consumer integrates by importing/replaying the fixtures — never by re-implementing the producer's logic
  (one-derivation-many-consumers, doctrine seed D-ONEDERIVE).

## Peer constraints overridden (MANDATORY section — may be "none")
> List every locked pin / standing decision / prior ruling **of the consuming repo(s)** this contract overrides
> or pre-decides, each with the consumer document cited and the explicit signature that authorized the override.
> **A contract citing no document from the consuming repo is rejected on form** — the recorded failure mode is a
> ratification that silently pre-decided two open founder-gated questions on the consumer's side.
- <consumer doc + pin overridden + authorizing signature>  *(or "none")*

## Credentials at spec time
- Credentials/tokens/scopes a consumer needs to exercise this seam: <named HERE, requested day one — founder
  acts are the long pole; a seam whose credential arrives at integration time blocks the lane>

## Contract tests
- <where they live; what they assert>
