---
description: Log + triage a founder friction report (runs the friction-triage skill)
---
Run the `friction-triage` skill on this report. Capture it VERBATIM first into `docs/friction-log.md`
(never paraphrase at intake), then classify F/W/U, run the four diagnostics, name the root cause (not the
symptom), link the fix lineage, and leave closure founder-gated.

Report: $ARGUMENTS
