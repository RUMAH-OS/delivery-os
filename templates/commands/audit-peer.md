---
description: Read-only reality audit of a sibling repo's ACTUAL shipped state (runs the cross-system-reality-audit skill)
---
Run the `cross-system-reality-audit` skill against the named peer repo: read its code, migrations, contracts,
and VERIFY artifacts on disk (read-only), and emit the audit-citation line + a delta report vs whatever the
registries/routers claim. Any plan gating on this peer must carry the citation (DoD row 8).

Peer: $ARGUMENTS
