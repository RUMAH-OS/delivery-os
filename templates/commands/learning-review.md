---
description: Phase-end retrospective that converts experience into routed, implemented changes (runs the learning-review skill)
---
Run the `learning-review` skill for the named phase. Reconstruct from artifacts (never memory), build the
assumptions ledger and pattern census, then apply the BLAST-RADIUS FORK: project-local findings implement in
the same commit series; OS-base/cross-system findings become an OS-FEEDBACK triage (design-first — never a
direct base write). Every used skill bumps its version or declares "no learning."

Phase: $ARGUMENTS
