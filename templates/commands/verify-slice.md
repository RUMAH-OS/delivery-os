---
description: Run an independent adversarial verification of a slice (runs the verification skill)
---
Run the `verification` skill as an INDEPENDENT verifier (a distinct invocation from whoever authored the
code — author≠verifier, Governance §12). Verify on the slice's REAL surface, build your own fixtures, mutate
adversarially, and write `docs/verify/VERIFY-<slice>.md` with verbatim evidence, a `machine_probe`, and an
honest verdict (`verified` only if everything passes; otherwise `executed` + bugs filed author-ward).

Slice: $ARGUMENTS
