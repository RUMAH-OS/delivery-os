# Archived skills (archive-with-pointer — nothing silently deleted)

Retired from the active catalog at v4.0. The files are kept verbatim; the lessons live elsewhere.

| Skill | Disposition (v4.0) | Where the lesson lives now |
|---|---|---|
| `grill-me` | **Retired from the default install** (X2): zero artifacts ever produced across two consumers and the framework itself — the existed-never-fired class. **Resurrection condition:** if the discovery phase recurs on a future project, rebuild it on the reference catalog's interview-me mechanics (S17 — strictly better than this zero-artifact form). | trigger-hierarchy doctrine (`skills/README.md`); S17 catalog note |
| `production-readiness-review` | **Folded into `principle-11-review` v2.0 as a decision class** (X3): the *function* ran at a real go-live through bespoke readiness docs while this *skill* sat idle — the missing piece was the **trigger** (operating begins at a named cutover, K25), not the checklist. The checklist survives as `templates/release-readiness.md` + `checklists/release-cutover.md`. | Governance §11 (production-readiness class + named-cutover trigger); `skills/principle-11-review/` |

Validator note: `_archive/` is skipped by `validate-skills.mjs` (directories starting with `_`).
